{
  "name": "@sitidos/cloudflare-client",
  "version": "0.1.0",
  "private": true,
  "description": "F9 — Typed wrapper around the Cloudflare API for DNS write, cache purge, and WAF rule list. Per-service scoped tokens.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.19.41",
    "typescript": "^5.5.4",
    "vitest": "^2.0.0"
  }
}
